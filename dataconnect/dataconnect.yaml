specVersion: "v1"
serviceId: "demoproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "demoproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
